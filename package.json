{
  "name": "data-backup-tool",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:front": "cd front && yarn install",
    "install:back": "cd back && yarn install",
    "install:all": "yarn install:front && yarn install:back",
    "dev:front": "cd front && yarn dev",
    "dev:back": "cd back && yarn dev",
    "build:front": "cd front && yarn build",
    "start": "cd back && yarn start"
  },
  "workspaces": [
    "front",
    "back"
  ]
}
